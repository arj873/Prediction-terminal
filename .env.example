# All optional — the terminal runs with none of these set.

# API server bind. HOST=0.0.0.0 to expose it beyond localhost.
PORT=8787
HOST=127.0.0.1

# Max API calls per client IP per minute.
RATE_LIMIT=600

# Whether X-Forwarded-For / X-Real-IP may name the client for rate limiting.
#
# Leave this off unless a proxy you control actually rewrites the chain. With
# it on and nothing in front of the server, a caller varies the header and gets
# a fresh bucket per request, which is the whole of the limit defeated.
# TRUST_PROXY=1

# FRED data is scraped from fred.stlouisfed.org by default. That host resets
# connections from datacentre and cloud IPs, so a hosted deployment often
# cannot scrape it at all. Setting a key (free, from
# https://fred.stlouisfed.org/docs/api/api_key.html) adds a fallback to the
# official API, which does answer from those networks. The scrape is always
# tried first.
# FRED_API_KEY=

# The news wire (`NEWS`) reads Alpaca's news API, which is the one feed here
# that needs a credential — no unauthenticated equity news feed exists that is
# both licensed and stable enough to parse. Keys are free from alpaca.markets;
# a paper-trading account issues a pair, and this server only ever sends them to
# data.alpaca.markets to read news. Every other command works without them.
# Alpaca's own SDK names (APCA_API_KEY_ID / APCA_API_SECRET_KEY) are read too.
# ALPACA_API_KEY_ID=
# ALPACA_API_SECRET_KEY=

# Upstream overrides. Mainly for testing against a fixture server.
# KALSHI_API_BASE=https://api.elections.kalshi.com/trade-api/v2
# FRED_WEB_BASE=https://fred.stlouisfed.org
# FRED_API_BASE=https://api.stlouisfed.org/fred
# ALPACA_DATA_BASE=https://data.alpaca.markets/v1beta1

# Polymarket International splits its public API across three hosts: the
# catalogue, the CLOB (books and price history) and the print tape.
# POLYMARKET_GAMMA_BASE=https://gamma-api.polymarket.com
# POLYMARKET_CLOB_BASE=https://clob.polymarket.com
# POLYMARKET_DATA_BASE=https://data-api.polymarket.com

# Polymarket US serves catalogue, book and BBO from a public gateway. Its price
# history and trade tape live on api.polymarket.us behind an API key, which this
# terminal deliberately does not hold — GP and TAS on a pmus: market say so.
# POLYMARKET_US_API_BASE=https://gateway.polymarket.us

# Gemini serves its prediction-market catalogue from the web host and its book,
# tape and candles from the same api.gemini.com the crypto exchange uses.
# GEMINI_CATALOGUE_BASE=https://www.gemini.com
# GEMINI_API_BASE=https://api.gemini.com

# predict.fun documents a REST API at api.predict.fun that requires an account
# key on every path, including its own "get the message to sign" step. Its web
# app does not use it: the public data is on a GraphQL host that needs no
# credential, and that is what this terminal reads. Send no Origin header — the
# host answers a wrong one with a hard 403.
# PREDICTFUN_GRAPHQL_BASE=https://graphql.predict.fun/graphql

# ForecastEx serves an undocumented JSON API from its own site, and publishes
# its end-of-session archives — the print tape and the daily bars — as CSV in a
# public S3 bucket. Both are read; neither needs a credential.
# FORECASTEX_API_BASE=https://forecastex.com
# FORECASTEX_ARCHIVE_BASE=https://forecastex-public-data.s3.amazonaws.com

# Spot prices. Equities are read from Yahoo first and Nasdaq second; Yahoo
# returns 429 to shared datacentre addresses, which is what the fallback is for.
# Crypto comes from Coinbase, which 403s a request with no User-Agent.
# YAHOO_API_BASE=https://query1.finance.yahoo.com
# NASDAQ_API_BASE=https://api.nasdaq.com
# COINBASE_API_BASE=https://api.exchange.coinbase.com

# Where the built client lives. The server serves it as static files with an
# SPA fallback, so production is one process on one port. Unset in development,
# where Vite serves the client on :5173 and proxies /api here.
# CLIENT_DIR=client/build

# Log filter, in tracing-subscriber's syntax (error|warn|info|debug|trace).
# RUST_LOG=info


# The data publishers behind ECO, ECOS and SRC. All optional; `SRC` says which
# of them this deployment can serve and what each missing key would add.
#
# The BLS answers anonymously at 25 queries a day and 10 years of history a
# call; a free key raises both. The EIA answers nothing at all without one.
# Congress.gov and data.gov fall back to api.data.gov's shared DEMO_KEY, which
# is throttled per IP. Polygon is not browsable — a key puts a licensed tape in
# front of Yahoo and Nasdaq for STK, CRY and IMP.
# BLS_API_KEY=
# EIA_API_KEY=
# CONGRESS_API_KEY=
# DATAGOV_API_KEY=
# POLYGON_API_KEY=

# The SEC's fair-access policy asks every caller to identify itself, and its
# hosts answer a request that does not with a 403. Not a credential — nothing is
# issued and nothing is checked — so set it to a real contact address.
# SEC_USER_AGENT=prediction-terminal (contact: you@example.com)

# Publisher upstreams. Mainly for testing against a fixture server.
# BLS_API_BASE=https://api.bls.gov/publicAPI
# ECB_API_BASE=https://data-api.ecb.europa.eu/service
# IMF_API_BASE=https://api.imf.org/external/sdmx/2.1
# OECD_API_BASE=https://sdmx.oecd.org/public/rest
# FED_DDP_BASE=https://www.federalreserve.gov/datadownload
# EIA_API_BASE=https://api.eia.gov/v2
# CFTC_API_BASE=https://publicreporting.cftc.gov/resource
# CONGRESS_API_BASE=https://api.congress.gov/v3
# SEC_DATA_BASE=https://data.sec.gov
# SEC_WWW_BASE=https://www.sec.gov
# DATAGOV_API_BASE=https://api.gsa.gov/technology/datagov/v4
# POLYGON_API_BASE=https://api.polygon.io
