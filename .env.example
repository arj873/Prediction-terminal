# All optional — the terminal runs with none of these set.

# API server bind. HOST=0.0.0.0 to expose it beyond localhost.
PORT=8787
HOST=127.0.0.1

# Max API calls per client IP per minute.
RATE_LIMIT=600

# FRED data is scraped from fred.stlouisfed.org by default. That host resets
# connections from datacentre and cloud IPs, so a hosted deployment often
# cannot scrape it at all. Setting a key (free, from
# https://fred.stlouisfed.org/docs/api/api_key.html) adds a fallback to the
# official API, which does answer from those networks. The scrape is always
# tried first.
# FRED_API_KEY=

# The news wire (`NEWS`) reads Alpaca's news API, which is the one feed here
# that needs a credential — no unauthenticated equity news feed exists that is
# both licensed and stable enough to parse. Keys are free from alpaca.markets;
# a paper-trading account issues a pair, and this server only ever sends them to
# data.alpaca.markets to read news. Every other command works without them.
# Alpaca's own SDK names (APCA_API_KEY_ID / APCA_API_SECRET_KEY) are read too.
# ALPACA_API_KEY_ID=
# ALPACA_API_SECRET_KEY=

# --- reference data publishers -------------------------------------------
# Ten of the twelve work with no key at all. Run `SRC` in the terminal to see
# what this deployment can currently serve, and what each missing key adds.

# The EIA publishes NOTHING anonymously — without this the source declares
# itself unavailable rather than failing at request time. Free and instant from
# https://www.eia.gov/opendata/register.php
# EIA_API_KEY=

# Polygon.io is the one key that improves something already working: it puts a
# licensed tape in front of Yahoo and Nasdaq for STK, CRY and IMP, which fixes
# the 429s Yahoo returns to datacentre IPs and the missing S&P 500 at Nasdaq.
# The free tier is end-of-day only; the terminal says so rather than showing an
# empty intraday chart. https://polygon.io/dashboard/api-keys
# POLYGON_API_KEY=

# The BLS answers unregistered callers 25 times a day with 10 years of history
# per call. A free key raises that to 500 a day and 20 years, and adds the
# series title and units from the BLS's own catalogue.
# https://data.bls.gov/registrationEngine/
# BLS_API_KEY=

# Congress.gov and data.gov both fall back to api.data.gov's shared DEMO_KEY,
# which works but is throttled per IP.
# https://api.congress.gov/sign-up/  and  https://api.data.gov/signup/
# CONGRESS_API_KEY=
# DATAGOV_API_KEY=

# EDGAR's fair-access policy asks callers to declare themselves as
# `Name contact@domain`. Note the shape: a browser-style User-Agent containing
# parentheses or a semicolon is refused with 403.
# SEC_USER_AGENT=your-project you@example.com

# Upstream overrides. Mainly for testing against a fixture server.
# KALSHI_API_BASE=https://api.elections.kalshi.com/trade-api/v2
# FRED_WEB_BASE=https://fred.stlouisfed.org
# FRED_API_BASE=https://api.stlouisfed.org/fred
# ALPACA_DATA_BASE=https://data.alpaca.markets/v1beta1

# Polymarket International splits its public API across three hosts: the
# catalogue, the CLOB (books and price history) and the print tape.
# POLYMARKET_GAMMA_BASE=https://gamma-api.polymarket.com
# POLYMARKET_CLOB_BASE=https://clob.polymarket.com
# POLYMARKET_DATA_BASE=https://data-api.polymarket.com

# Polymarket US serves catalogue, book and BBO from a public gateway. Its price
# history and trade tape live on api.polymarket.us behind an API key, which this
# terminal deliberately does not hold — GP and TAS on a pmus: market say so.
# POLYMARKET_US_API_BASE=https://gateway.polymarket.us

# Reference-data upstreams, same purpose.
# BLS_API_BASE=https://api.bls.gov/publicAPI
# ECB_API_BASE=https://data-api.ecb.europa.eu/service
# OECD_API_BASE=https://sdmx.oecd.org/public/rest
# IMF_API_BASE=https://api.imf.org/external/sdmx/2.1
# FED_DDP_BASE=https://www.federalreserve.gov/datadownload
# EIA_API_BASE=https://api.eia.gov/v2
# CFTC_API_BASE=https://publicreporting.cftc.gov/resource
# CONGRESS_API_BASE=https://api.congress.gov/v3
# DATAGOV_API_BASE=https://api.gsa.gov/technology/datagov/v4
# SEC_DATA_BASE=https://data.sec.gov
# SEC_WWW_BASE=https://www.sec.gov
# POLYGON_API_BASE=https://api.polygon.io
