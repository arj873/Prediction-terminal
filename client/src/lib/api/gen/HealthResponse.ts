// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { VenueCorpusHealth } from "./VenueCorpusHealth";

export type HealthResponse = { ok: boolean, uptimeSeconds: number, 
/**
 * Which optional feeds this deployment can serve. Not a secret — it is the
 * difference between "this terminal cannot do that" and "that is broken",
 * and an operator needs it. The cache counters that used to sit beside
 * these are gone: nothing read them, and they described internal traffic
 * to anyone who asked.
 */
fredApiKey: boolean, alpacaKeys: boolean, time: string, 
/**
 * What each venue's catalogue crawl last did.
 *
 * Here because `SRCH` returning nothing has two completely different
 * causes — no market matches, or no catalogue was ever fetched — and an
 * operator on a hosted deployment cannot tell them apart from the outside.
 * Every venue is listed every time: one silently absent from the list is
 * the ambiguity this exists to remove.
 */
venues: Array<VenueCorpusHealth>, };
